package ws.billdavis.utilities.conversion;

public class ConvertUnicodeOrgPostalCodeRegExFileTest {
    /*
    open file stream
    get down to the first record
    read records
          update db (Create or Update)

    responsiblities:
        main (parse c/l, open file, pass to parser, run rest)
        parser (parse xml)
        db
     */

}
